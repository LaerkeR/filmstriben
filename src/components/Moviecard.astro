---
const { aarstal, filmTitle, slug, varighed, genre, imgSrc, undergenre, point } =
  Astro.props;
---

<a href={`${slug}`}>
  <article
    class="aspect-ratio relative w-full flex flex-col-reverse h-[60vw] md:h-[40vw] lg:h-[20vw] overflow-hidden bg-no-repeat bg-cover bg-top"
    style={`background-image: url("${imgSrc}")`}
  >
    <div class="w-full bottom-0">
      <!-- <div class="bg-black top-[40%] blurShadow blur-[10px]"></div> -->
      <div class="bg-black bg-opacity-75 flex flex-col gap-2 px-5 pb-5">
        <p class="font-display text-xs">{filmTitle}</p>
        <div class="infoBar text-white flex gap-2 items-center body-sm">
          <p class="varrighed body-xs">{varighed}</p>|<p
            class="aarstal body-xs"
          >
            {aarstal}
          </p>|<p class="genre body-xs">{genre}</p>|<p class="genre body-xs">
            {point} point
          </p>
        </div>
      </div>
    </div>
  </article>
</a>

<script>
  export default {
    props: ["film"],
  };
</script>
