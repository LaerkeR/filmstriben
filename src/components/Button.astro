---
const { tekst, variant, filtrering } = Astro.props;
---

<button data-variant={variant}>
  <h4 
    class="bg-yellow-500 text-yellow-900 py-3 px-5 rounded-md border border-yellow-500
           hover:bg-yellow-100 hover:text-yellow-500">
    {tekst}
  </h4>
</button>

<button data-variant={filtrering}>
  <h4 
    class="bg-black text-yellow-500 py-3 px-5 rounded-md border border-yellow-500
    hover:bg-yellow-500 hover:text-yellow-900">
    {tekst}
  </h4>
</button>